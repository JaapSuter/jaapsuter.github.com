toggleBaseline = ->
  blcb = $($('#baseline-checkbox'))
  blcb['attr']('checked', !blcb['attr']('checked'))
  return
  
onWindowLoad = =>
  return if top != window

  onKeyUp 'B', toggleBaseline
  # onKeyUp 'T', getTypeMetrics
  
  # $(window).bind('popstate ', onPopState)
  # $('#type-metrics-link').bind 'click', type.getMetrics

  # bindInternalAnchorClicks()
  
# $(window).bind('load', onWindowLoad) if top == window
onWindowLoad()
